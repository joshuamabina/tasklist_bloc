name: tasklist_bloc
description: "A basic introduction to state management in flutter apps using BLoC (Business Logic Component) design pattern."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  collection: ^1.18.0
  flutter_bloc: ^8.1.3
  tasklist_api:
    path: packages/tasklist_api
  local_storage_tasklist_api:
    path: packages/local_storage_tasklist_api

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  test: ^1.24.9

flutter:
  uses-material-design: true
