name: local_storage_tasklist_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  tasklist_api:
    path: ../tasklist_api

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
